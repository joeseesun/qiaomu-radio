# 乔木电台设计系统

## 功能契约
页面只有右上角单枚主题图标和一台完整播放器。点击图标才展开主题。初次访问默认博朗 3D；主题切换自动播放关联系列。屏外不放介绍、心情列表、推荐侧栏、数据统计或页脚。搜索、频道、电台列表、喜欢、不喜欢、历史、音量、来源与错误恢复全部在播放器内部完成。

## 视觉
- iPod：白色圆角机身、顶部 hold 开关、绿色单色液晶、完整 click wheel；参考用户附件的长宽比例、屏幕内框、圆盘和机身阴影。
- Winamp：深色像素窗口、凹陷显示区、频谱与凸起传输按钮。
- foobar2000：紧凑灰色桌面窗口、工具栏、列表与底部传输控制。
- 极简：干净的浅色一体播放器。
- 收音机：棕色边框、模拟刻度、机内屏幕与旋钮式控制。
- 魔兽世界 3D：Hyper3D 生成的铁木机身、红蓝双扬声器和中央黑玻璃屏幕；屏幕由实时 CanvasTexture 覆盖，模型上的上一家、播放/暂停、下一家、收藏与音量旋钮直接控制真实播放器。

视觉冒险度 7/10，动效 3/10；默认界面信息密度低，菜单内密度适中。每个主题拥有完整的环境背景与机身，不在主题外复制控制或当前电台信息。博朗 3D 以轻微右上方透视默认展示，保留正面可读性并显示机身厚度；点击屏幕靠近机内菜单。菜单使用 CSS3D 与玻璃同位置、同透视，不出现独立弹层。简易控制与模型外的常驻视角按钮已移除；恢复视角、拆解展示和支持入口都收入机内屏幕。

博朗 3D 直接操控契约：大旋钮围绕轴心连续多圈调谐，每圈 20 档并即时换台，按下播放/暂停；小旋钮有 270° 行程，面板固定刻度弧、两端加长止挡线与 −/+ 标记，旋钮指针随真实音量转动。到头停止，反向立即响应；按下静音保留指针角度，再次按下恢复。中心或极侧面拖动提供线性操作兜底，滚轮可精调。橙色键按压播放/暂停。命中部件后锁定手势，旋钮拖动不改变观察视角。机身使用四元数自由翻转，自动拉远保持完整取景；地面、光源不随之旋转，包围盒约束防止穿地，屏幕背向观察者时隐藏并禁用交互。阴影采用真实投影，不用渐变贴片。播放时喇叭网罩与背板保持固定，不复制魔兽主题的可见振膜效果；状态由机内屏幕和橙色键的克制亮起表达。探索机身中的拆解为可选展示，恢复视角仅在偏离默认取景后出现。键盘聚焦机身后：左右调台、Enter 确认、上下音量、M 静音、空格播放、Home 复位。

魔兽世界 3D 直接操控契约：保留原有红蓝魔幻模型，视觉冒险度 7、动效强度 3、默认信息密度 2。黑玻璃是唯一信息表面；默认不重复显示模型外标题和播放器。歌曲/电台采用投射到实际玻璃的圆角 CanvasTexture 面，机内菜单采用同一位置的 CSS3D，点击玻璃靠近阅读，返回时恢复机身。五个实体控件依次执行上一家、播放/暂停、下一家、收藏和音量；射线只取最近的真实表面，不从背后穿透命中。按钮前部顶点按压/回弹，短暂的红、蓝、金反馈贴着各自表面；播放和收藏状态保留低强度亮起。音量可以绕圈、中心拖动或滚轮调节，旋钮前部与纹理指针随数值转动，按下静音保留角度。模型仍为单网格资产，应用按空间范围绑定顶点，不伪称已经 BANG 拆件。辅助控制与视角工具点击页面后渐进显露，4 秒无操作淡出；完整简易播放器仅主动展开或模型失败时显示。

本轮魔兽世界 3D 打磨签名细节：金属框内圆角屏幕、分层暖白文字、贴合的符文反馈、几何按压回弹、金色音量读数条、品牌焦点与机内细滚动条。复用现有红蓝金色资产与博朗 3D 的直接操控原则，不改成另一套网页风格。搜索/列表切页重置内部滚动，输入焦点不能把页面拖出机身。

魔兽世界 3D 动效补充：按压采用短行程、快速压下与平顺归位；不足一帧的轻点也呈现确认行程。光效与按钮顶点使用同一权重，避免按下后光层穿入按钮。红蓝喇叭只驱动内侧振膜，固定装甲不动；播放且有音量时柔和启动，暂停、缓冲或静音后衰减归位。除实体顶点前后位移外，振膜表面还有与位移同步的径向缩放和低透明光反射，解决烘焙纹理模型中纯 Z 轴位移肉眼不可见的问题。它是播放状态驱动的氛围运动，不是歌曲节拍或实时频谱。减少动态效果设置即时关闭持续运动。

魔兽世界 3D 加载态只显示带真实 Alpha 通道的透明符文，不显示文字，也不在符文后叠加进度圆盘；加载进度仍通过无障碍 `progressbar` 暴露。

## 机内导航
博朗 3D 建模策略：全部机身由 Three.js 参数化装配，暖灰圆角外壳、独立前后盖、四个落地脚垫、规则网孔、银色/炭黑/橙色控制件。前盖嵌入外壳搭接边，不暴露黑色密封垫；网板与面板齐平，背板退入孔后。物理模型不请求 GLB 或纹理文件；机内文字继续使用 CSS3D，播放逻辑不迁入 WebGL。保留原有比例与操作位置，不增加常驻工具或装饰。验收覆盖前/侧面、屏幕对齐、实体命中、拆解复位、窄屏和资源请求。

iPod 默认显示当前电台，MENU 打开菜单，左右键移动选项，中心键选择，MENU 返回；菜单列表也支持直接点击。正在播放时左右键为前后电台，圆盘滚轮调整音量。其他主题使用机内图标工具栏访问相同页面。搜索结果、收藏、历史在液晶/窗口内部滚动。

## 约束
- 所有真实状态以播放事件为依据，不伪造码率、曲名或专辑。
- 首次访问且没有已保存主题时，服务端按 IP 国家返回当地高票音乐电台；只使用国家级结果。定位失败或当地音乐台过少时使用跨国家的全球热门 20 台，不以中国列表作为全球缺省回退。
- HLS 不做持久录音缓存。浏览器采用 8 秒直播延迟、45 秒滚动缓冲与两级原地恢复；项目内审核的短窗口 HLS 源由服务端提前抓取分片并在内存中短时复用，再失败才自动换台。
- 界面提供简体中文、英语、西班牙语、法语、德语、日语，默认跟随浏览器并可在机内菜单切换；国家名本地化，电台固有名称不翻译。
- 网络失败提示在屏幕内；最多尝试 5 家，失败可重试。快速换主题只消费最后一次目录请求。
- 中文系统字体；长台名最多三行，列表省略但可通过选中收听显示全名。
- 顶部导航不抢主视觉。完整选中背景、focus-visible，不用装饰性左竖线。
- 1440 × 900 和 390 × 844 验收；矮屏可整体滚动，列表内部独立滚动。
- reduced-motion 关闭频谱动画。

## 本轮验收
- 魔兽世界 3D 在 1280 × 720 桌面视口与 626 × 828 窄视口完成真实渲染；模型、动态屏幕、播放、切台、收藏与滚轮音量均实际操作通过，桌面最终页无控制台错误或警告。626px 是窄屏验证，不等同于 390px 真机。
- 桌面 1440 × 900、移动 390 × 844：完整 iPod 机身位于视口内，无页面横向溢出。
- 机内频道切中国源、搜索北京、收藏/取消收藏、历史内部滚动、音量操作已通过真实浏览器操作。
- iPod 与 Winamp 自动开播；连续切换主题后停留在最后选择的收音机，并播放北京音乐广播。
- 第一轮发现长台名和频谱会挤出收藏按钮；第二轮把操作行固定在屏幕底部，内容独立滚动。
- npm run check 通过（6 个测试、TypeScript 与生产构建）。构建仍提示 HLS 主包较大。
